---
const basePath = import.meta.env.BASE_URL.endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const demoUrl = `${basePath}demo.html`;
---

<!-- The supplied workspace demo stays in this page, fully interactive. -->
<section id="product-tour" class="border-b border-blue-deep bg-blue-deep scroll-mt-20">
  <div class="mx-auto max-w-[1200px] px-4 py-16 sm:px-6 sm:py-20">
    <div class="max-w-[52ch]">
      <p class="u-label text-agent">Product tour</p>
      <h2 class="u-h2 tour-title mt-4 text-ground">A request becomes a pipeline.</h2>
      <p class="u-lead mt-4 !text-tint">
        A team needs a nightly file of student contact details. Akaden turns that request into a pipeline they can review and run.
      </p>
    </div>

    <div class="workspace-preview mt-8 min-w-0">
      <iframe
        src={demoUrl}
        title="Interactive Akaden workspace demo"
        loading="lazy"
        data-workspace-demo
      ></iframe>
    </div>
    <p class="mt-3 text-[0.8125rem] text-tint/80">
      Recorded walkthrough. No live tenant, student data, or production changes.
    </p>
  </div>
</section>

<style>
  .workspace-preview {
    overflow: hidden;
    border: 1px solid color-mix(in srgb, var(--color-tint) 56%, transparent);
    border-radius: 8px;
    background: #fff;
    box-shadow: 0 20px 50px rgba(0, 0, 0, .28);
  }
  .tour-title{white-space:nowrap;font-size:clamp(1.6rem,3.1vw,2.75rem);}
  .workspace-preview iframe {
    display: block;
    width: 100%;
    height: min(58rem, 100dvh);
    min-height: 34rem;
    border: 0;
    background: #fff;
  }
  @media (max-width: 560px){.tour-title{white-space:normal;}}
</style>

<script>
  const workspaceDemo = document.querySelector<HTMLIFrameElement>('[data-workspace-demo]');

  function fitDemoToContents() {
    const documentInFrame = workspaceDemo?.contentDocument;
    if (!workspaceDemo || !documentInFrame) return;
    const height = Math.max(
      documentInFrame.body.scrollHeight,
      documentInFrame.documentElement.scrollHeight,
    );
    workspaceDemo.style.height = `${height}px`;
  }

  workspaceDemo?.addEventListener('load', () => {
    fitDemoToContents();
    const documentInFrame = workspaceDemo.contentDocument;
    if (!documentInFrame?.body) return;
    new ResizeObserver(fitDemoToContents).observe(documentInFrame.body);
  });
</script>
